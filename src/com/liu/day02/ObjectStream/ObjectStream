用一个字节序列（包含对象的数据，对象的类型，对象中存储的数据等信息）可以表示一个对象  字节序列写出文件之后，相当于文件持久保存了一个对象的信息，这就是序列化
反之，字节序列可以从文件中读取回来，重构对象，进行反序列化

一个对象想要被序列化必须满足两个条件：
1.该类必须实现Serializable接口；
2.该类的所有属性必须是可序列化的，如果有属性不需要被序列化，该属性必须注明是瞬态的，用transient关键字修饰
